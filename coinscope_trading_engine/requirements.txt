# =============================================================================
#  CoinScopeAI Trading Engine — Python Dependencies
#  Install:  pip install -r requirements.txt
#  Python:   3.11+
# =============================================================================

# ── Web framework & server ────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9        # form/file uploads in FastAPI
httpx>=0.27.0                  # async HTTP client (webhooks, external APIs)

# ── Configuration & environment ───────────────────────────────────────────────
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1

# ── Binance exchange connectivity ─────────────────────────────────────────────
python-binance>=1.0.19         # REST + WebSocket helpers
websockets>=12.0               # raw WebSocket (binance_websocket.py)
aiohttp>=3.9.5                 # async HTTP session (binance_rest.py)

# ── Caching & messaging ───────────────────────────────────────────────────────
redis>=5.0.3
aioredis>=2.0.1

# ── Distributed task processing (Celery) ─────────────────────────────────────
celery[redis]>=5.3.6           # Celery broker + result backend via Redis
kombu>=5.3.3                   # Celery message transport (auto-installed with celery)
flower>=2.0.1                  # Celery web monitor  (celery -A celery_app flower)

# ── Alternative WebSocket backends (optional — pick one) ─────────────────────
# python-binance already listed above — covers Backend A
# Uncomment for UBWA backend (high-throughput multi-stream):
# unicorn-binance-websocket-api>=2.7.0

# ── Data processing & technical analysis ─────────────────────────────────────
pandas>=2.2.1
numpy>=1.26.4
ta>=0.11.0                     # Technical Analysis (RSI, MACD, BB, ATR, etc.)
scipy>=1.13.0                  # Statistical helpers (correlation, regression)
scikit-learn>=1.4.1            # ML utilities, anomaly detection

# ── Deep learning / ML models ────────────────────────────────────────────────
torch>=2.2.2                   # LSTM price predictor
torchvision>=0.17.2            # Required by some torch pipelines
transformers>=4.40.0           # HuggingFace — FinBERT sentiment analyser
hmmlearn>=0.3.2                # Hidden Markov Model regime detector

# ── Alerts & notifications ────────────────────────────────────────────────────
python-telegram-bot>=21.1.1

# ── Database ─────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.29
aiosqlite>=0.20.0              # Async SQLite driver (dev/test)
asyncpg>=0.29.0                # Async PostgreSQL driver (production)
alembic>=1.13.1                # Database migrations

# ── Monitoring & observability ────────────────────────────────────────────────
prometheus-client>=0.20.0      # Metrics exporter
python-json-logger>=2.0.7      # Structured JSON logging

# ── Cryptography (request signing) ───────────────────────────────────────────
cryptography>=42.0.5           # Ed25519 key support for session.logon

# ── Billing ───────────────────────────────────────────────────────────────
stripe>=7.0.0                  # Stripe payments SDK

# ── Utilities ────────────────────────────────────────────────────────────────
python-dateutil>=2.9.0
pytz>=2024.1
ujson>=5.9.0                   # Fast JSON serialisation
tenacity>=8.2.3                # Retry logic helpers

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.1.1
pytest-asyncio>=0.23.6
pytest-cov>=5.0.0
pytest-mock>=3.14.0
respx>=0.21.1                  # Mock httpx requests in tests
freezegun>=1.4.0               # Freeze time in tests
