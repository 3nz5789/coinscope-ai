# CoinScopeAI Weekly Digest — Monday 2026-05-04 (Asia/Amman)

**Window:** 2026-04-27 to 2026-05-04 UTC
**Validation phase:** Day 4 of 30 (P0 cohort opened 2026-05-01)
**Disclaimer:** Testnet only. 30-day validation phase. No real capital.

> Note: scheduled task ran one day late vs. the nominal Sunday 8am Asia/Amman cadence. The 2026-05-03 digest already covered the 2026-04-26 → 2026-05-03 window; this digest re-anchors on the rolling 7-day window ending 2026-05-04 and surfaces the one new decision (2026-05-03 max-open-positions revision) plus a refreshed engine + drift snapshot.

## 1. Decisions

The decision-log carries 63 entries inside the 7-day window. 61 are dated 2026-05-01 (the v1 framework lock pass — fully covered in the 2026-05-03 digest); 1 is dated 2026-05-02 (protective tooling); 1 is dated 2026-05-03 (max-open-positions amendment).

- **2026-05-03 — max-open-positions-revised-3-to-5 (risk).** Canonical `max_open_positions` raised from 3 → 5 to match the running engine config. Eliminates the "running > ceiling" paradox flagged in the architecture v5 risk table. Cascades: CLAUDE.md amended, `.env*` reverted from 3 to 5 with updated comment, `risk_threshold_guardrail.py` rule renamed `open_positions_above_3` → `open_positions_above_5`, design-system-manifest §3 updated, drift_detector `CANONICAL_TOKENS` → 5, Notion Risk & Governance + Knowledge Base body refresh queued. Other risk thresholds unchanged (10x leverage, 10% drawdown, 5% daily loss, 80% heat cap). Single-token amendment, not framework revision.
- **2026-05-02 — protective-tooling-and-design-system-canonicalization (ops + branding).** Shipped four protective + workflow capabilities: `drift_detector.py` (10 canonical .md files), `risk_threshold_guardrail.py` (`.py`/`.env*`/`.yml`/`.json` codebase scan — caught + patched 4 real violations), `daily_status.sh` (Engine API morning brief), and the `coinscope-weekly-digest` scheduled task itself. Also canonicalized `architecture/design-system-manifest.md` as the live source of truth (Apr-25 zip retired to visual-snapshot status). 10 redundant local items archived; 7 stale Drive items trashed.
- **2026-05-01 framework lock (61 entries).** §1–§16 all v1 LOCKED on the same day, including: §3 sub-persona re-cut to E (Omar/Karim/Layla); §4.4 lead VP "AI-driven capital-preservation infrastructure that enforces the discipline you've already built"; §5/§5.2 three-tier matrix locked (Free / Trader $79 / Desk Preview $399 / Desk Full v2 $1,199 + per-seat); §11 M24 ARR ~$1.9M; §14 six stop-the-line conditions; PCC v2 §8 leverage 10x supersedes earlier 20x. See 2026-05-03 digest §1 for the structural call enumeration.

## 2. Code reviews

Three daily reviews fell inside the window: 2026-04-30, 2026-05-01, 2026-05-02. (No reviews dated 2026-05-03 or 2026-05-04 — the daily-review cadence appears to have paused, consistent with the §1 recommendation to either resume engine work or annotate dormancy.) Top 3 takeaways from each:

**2026-04-30** — twelfth consecutive day with zero source changes in scope (HEAD `59416a2` from 2026-04-18). 7 capital-at-risk P0s carry over unchanged; two new P2s in `risk/circuit_breaker.py` (lurking concurrency hazards in current single-task topology). Recommendation repeated for the fourth day: stop adding findings, start clearing them, or annotate `README.md` that the engine is intentionally dormant during the Tier-1 doc restructure.

**2026-05-01** — thirteenth frozen day. **One new P1 surfaced** in `signals/signal_generator.py` §5.4 (shared HMM regime detector model-state bug — first time this file got a deeper spot-check); three new P2s (§5.5–§5.7). Two suffixed duplicate files still uncleaned: `whale_signal_filter.py` + `whale_signal_filter (1).py`, `trade_journal.py` + `trade_journal (2).py`.

**2026-05-02** — fourteenth frozen day. No new findings; deeper read of `signal_generator.py` at the same SHA produced no additional defects beyond 2026-05-01. P0/P1/P2 counts steady at 7/17/22. Ten untracked `CODE_REVIEW_*.md` files in repo root, still not committed, not triaged, not converted to issues. Sixth consecutive recommendation to either cap the daily review or freeze it explicitly.

## 3. Performance + risk events

Engine API unreachable at digest time (curl exit 7 — connection refused on `localhost:8001` for `/performance`, `/risk-gate`, and `/journal?limit=200`). Performance section skipped. No fabricated metrics.

Consistent with §0 of the code reviews and the 2026-05-03 digest: HEAD has been frozen at `59416a2` since 2026-04-18 and the engine is not actively serving the local API in this session. PnL, win rate, drawdown trajectory, gate-halt count, and rejection count all unavailable. Drawdown context against the 10x PCC v2 §8 Capital Cap and the 10% account hard-stop cannot be surfaced until the engine is back online.

## 4. Drift / consistency

- drift_detector: exit **0** / **0** findings ("No drift found across 10 canonical files.")
- risk_threshold_guardrail: exit **0** / **0** findings ("Risk thresholds clean across all scanned code.")

Both clean. The 10x leverage cap (PCC v2 §8) and the revised 5-position concurrency cap (2026-05-03) have held across the canonical files and the codebase. No 20x leverage residue and no `MAX_OPEN_POSITIONS=3` residue surfaced — the 2026-05-03 single-token amendment landed cleanly.

No Telegram alert dispatched: drift exit 0 and drawdown unknown (engine unreachable rather than ≥7%).

## Action items for the week ahead

1. **Bring the Engine API back online** before the validation cohort hits Day 7 of 30 — performance + risk-gate + journal must be reachable for any meaningful drawdown surfacing against the 10% account hard-stop. Two consecutive digests have skipped §3 entirely.
2. **Resolve the engine-frozen ambiguity.** Either resume engine work and clear the 7 carryover P0s (signed REST, liquidation REST normalisation, correlation gate, webhook HMAC, WS reconnect counter, alert-queue race, rate-limiter unbounded map), or annotate `README.md` that the engine is intentionally dormant during the Tier-1 restructure so the daily code-review task switches to a no-op verification (the cadence already appears to have paused after 2026-05-02 without an explicit decision).
3. **Commit the v1 framework + protective-tooling deliverables to git.** 63 decision-log entries plus the four 2026-05-02 scripts plus the design-system-manifest are still local-only per the 2026-05-02 code review. Push to GitHub branch `docs/business-plan-v1-2026-05-01` so the canonical contract and the safety net are recoverable.
