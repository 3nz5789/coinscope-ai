# CoinScopeAI Weekly Digest — Sunday 2026-05-03 (Asia/Amman)

**Window:** 2026-04-26 to 2026-05-03 UTC
**Validation phase:** Day 3 of 30 (P0 cohort opened 2026-05-01)
**Disclaimer:** Testnet only. 30-day validation phase. No real capital.

## 1. Decisions

The decision-log carries 63 entries inside the 7-day window — all dated 2026-05-01 — corresponding to the v1 framework lock pass. Rather than enumerate, the meaningful structural calls:

- **2026-05-01 §3 — Sub-persona re-cut to E (provisional).** Personas re-cut on a mixed origin/scale axis: P1 Self-Taught Methodist ("Omar") replaces Burned Survivor; P2 Engineer Trader ("Karim") unchanged; P3 Solo PM ("Layla") $200k–$1M aggregate book replaces Aspiring Pro. Internal names only.
- **2026-05-01 §4.3 / §4.4 — Problem statement and lead VP locked (Candidate D).** Lead VP: "AI-driven capital-preservation infrastructure that enforces the discipline you've already built." Anti-overclaim audit applied; "infrastructure" defended for P3, flagged for re-audit if stretched downstream.
- **2026-05-01 §5 + §5.2 — Tier matrix locked (Track B canonical).** Three-tier structure: Free / Trader / Desk. v1 ships Trader-full + Desk Preview (3-account cap, monthly PDF report); full Desk (multi-account + audit-grade exports + role seats) deferred to v2. Tax-ready exports + LP-style book gates pushed to v3.
- **2026-05-01 §6.6 — Tier prices v1 locked.** Free / Trader $79 / Desk Preview $399 / Desk Full v2 $1,199 + per-seat ($149 or $249).
- **2026-05-01 §8 v1 LOCKED + §1/§2/§7/§9/§10/§11/§12/§13/§14/§15/§16 v1 LOCKED.** Whole 18-section framework moved to LOCKED status; founder confirmation 2026-05-01. PCC v2 §8 Capital Cap fixes leverage at 10x (supersedes earlier 20x).
- **2026-05-01 §11 — Cost model + LTV/CAC LOCKED, M24 ARR ~$1.9M.** Free→Trader conversion locked, per-seat density time-varying, contractor-cost optionality flagged at P4.
- **2026-05-01 §14 — Public launch window locked; six stop-the-line conditions** (incl. new condition 6: persona invalidation triggers public-launch hold).

No decision-log entries dated 2026-05-02 or 2026-05-03 (the design-system-sync feedback and Drive-UI-automation rule on 2026-05-02 are operational memory entries, not framework decisions).

## 2. Code reviews

Four daily reviews fell inside the window: 2026-04-26, 2026-04-30, 2026-05-01, 2026-05-02. Top 3 takeaways from each:

**2026-04-26** — eighth consecutive day with zero source changes in scope (HEAD `59416a2` from 2026-04-18). All 7 capital-at-risk P0s carry over unchanged; one new P2 surfaced (`signals/indicator_engine.py:324` `_ema` SMA-seed bias). Six untracked `CODE_REVIEW_*.md` files accumulating in repo root with no triage destination.

**2026-04-30** — twelfth consecutive frozen day. 7 P0s + 16 P1s unchanged; two new P2s in `risk/circuit_breaker.py` (lurking concurrency hazards in current single-task topology). Recommendation repeated 4x: stop adding findings, start clearing them — or annotate `README.md` that the engine is intentionally dormant during the Tier-1 doc restructure so the daily review can downgrade to a no-op verification.

**2026-05-01** — thirteenth frozen day. **One new P1 surfaced** in `signals/signal_generator.py` §5.4 (shared HMM regime detector model-state bug — first time this file got a deeper spot-check); three new P2s. Two suffixed duplicate files still uncleaned: `whale_signal_filter.py` + `whale_signal_filter (1).py`, `trade_journal.py` + `trade_journal (2).py`.

**2026-05-02** — fourteenth frozen day. No new findings; deeper read of `signal_generator.py` at the same SHA produced no additional defects beyond yesterday's. P0/P1/P2 counts steady at 7/17/22. Nine `CODE_REVIEW_*.md` files in repo root, still untracked, still unconverted to issues. Sixth consecutive recommendation to cap the daily review or freeze it explicitly.

## 3. Performance + risk events

Engine API unreachable at digest time (curl exit 7 — connection refused on `localhost:8001` for `/performance`, `/risk-gate`, and `/journal`). Performance section skipped. No fabricated metrics.

Consistent with §0 of the code reviews: HEAD has been frozen since 2026-04-18 and the engine is not actively serving the local API in this session. PnL, win rate, drawdown, gate-halt count, and rejection count all unavailable.

## 4. Drift / consistency

- drift_detector: **0** / 0 findings ("No drift found across 10 canonical files.")
- risk_threshold_guardrail: **0** / 0 findings ("Risk thresholds clean across all scanned code.")

Both clean. The 10x leverage cap locked 2026-05-01 via PCC v2 §8 has held across the canonical files; no 20x residue surfaced.

## Action items for the week ahead

1. **Clear the code-review backlog** — convert at least the 7 P0s (signed REST, liquidation REST normalisation, correlation gate, webhook HMAC, WS reconnect counter, alert-queue race, rate-limiter unbounded map) into Linear issues or annotate `README.md` that the engine is intentionally frozen during the Tier-1 restructure so the daily review can switch to a no-op format. Ten untracked `CODE_REVIEW_*.md` files now sit in the repo root.
2. **Bring the Engine API back online** for the validation cohort window (Day 3 of 30) — performance + risk-gate + journal must be reachable for the digest to surface drawdown context against the 10x §8 Capital Cap.
3. **Commit the v1 framework lock** — 63 framework decisions landed 2026-05-01 but the corresponding `business-plan/` tree is still untracked per the 2026-05-02 review. Push to GitHub `docs/business-plan-v1-2026-05-01` so the canonical contract isn't local-only.
